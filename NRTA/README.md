# Directory Structure

ROOT:
**train.py** contains the 'train' class
**debugging.py** is the place to run the model and check its performance

BACKEND:
**packages.py** contains all the necessary packages
**config.pytion.ontains the `config` class
**propagation.py** contains the 'propagator' class
**utils.py** contains functions for plotting and shaping arrays
**model.py** contains the 'NVNet' class

DATA:
self-explanatory, contains data.

# MODEL EXECUTION